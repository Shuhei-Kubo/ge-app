.function-row
  %h2 投稿する
.form_newpost
  = form_with model: @post do |form|
    .form-title-box
      = form.label :タイトルを入力してください！（２０文字以内）
      = form.text_field :title, class: "title-input-form", autofocus: true, maxlength: "20"
    .form-text-box
      = form.label :気になることを質問しよう！（255文字以内）
      = form.text_area :text, rows: "5", class: "text-content", autofocus: true, maxlength: "255"
      = form.label :image, class: 'Form__inputImage' do
        = icon('far', 'image', class: 'Form__icon')
        = form.file_field :image, class: 'Hidden'
    .form-send-btn
      = form.submit "投稿する", class: "newpost_submitbtn"


